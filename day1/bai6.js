// Write a program asking user to enter a number, x, then check if x is in the lower half or
// higher half of 0 - 9 range
function Check()
    {
       let x = Number(prompt("Mời Nhập Một Số: "))
       if(x > 4.5) console.log("higher half of 9") 
       else console.log("lower half of 9")
    }